# Dependências para o webhook do Stripe
python-dotenv==1.0.0
firebase-admin==6.2.0
stripe==7.0.0
requests==2.31.0
pandas==2.0.3

# Dependências para segurança
cryptography==39.0.0 --only-binary=:all:
bcrypt==4.0.1 --only-binary=:all:

# Outras dependências essenciais
PyJWT==2.8.0
python-dateutil==2.8.2
pydantic==1.10.8
